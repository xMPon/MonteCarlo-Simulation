[tool.pytest.ini_options]
testpaths = ["."]
addopts = "-v --tb=short --cov=simulations --cov=finance --cov=exotics --cov-report=term-missing"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W"]
